import ArrayVector from './ArrayVector.mjs'

export default class MatrixGrayscale extends ArrayVector {
  /**
   * @param {Matrix} matrix
   * @param {Object|{ }} options
   * @returns {MatrixPadding|Vector[]}
   */
  create(matrix, options = { }) {
    for (let i = 0; i < matrix.length; i++) {

    }
    return this
  }
}
